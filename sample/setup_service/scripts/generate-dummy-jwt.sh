# TODO: call ../../common/scripts/generate-keys.sh and use a dummy JWT template and sign it.
# (or perhaps it should already be generated and we could use a static one?)

# TODO: we currently do not need this script anymore; delete if it's still the case

echo "Generating dummy JWT"